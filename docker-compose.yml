services:
  deep-research:
    container_name: deep-research
    build: .
    ports:
      - 4016:4016
    env_file:
      - .env.local
    volumes:
      -  ./:/app/
    tty: true
    stdin_open: true